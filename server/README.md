# Dependencies
You know the drill, `pip install -r requirements.txt`.

# API Keys
Create an `.env` file in the server folder and include API keys for `AIORNOT_API_KEY` and `OPENAI_API_KEY`.

# Run Server
Run the server using `python3 main.py` in the server folder.